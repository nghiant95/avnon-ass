import type { EntitySelectors, EntityState } from './typings';
export declare function createSelectorsFactory<TData>(): {
    getSelectors: {
        (): EntitySelectors<TData, EntityState<TData>>;
        <TState>(selectState: (state: TState) => EntityState<TData>): EntitySelectors<TData, TState>;
    };
};
